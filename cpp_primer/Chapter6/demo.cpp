#include <cstdio>
#include <iostream>
#include <cassert>
#include <string>
#include <vector>

using namespace std;

class A {
public:
	A() = default;
	~A() = default;
};

int main(void)
{
	vector<int> vec;
	bool (*pf)(const string &, const string &) = nullptr;
	return 0;
}
